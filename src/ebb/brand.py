BRAND = "foretop"
"""The insulation rule (NEXT_STEPS.md): until the trademark search clears, the brand name lives
in exactly this one constant — package prefix, CLI namespace, user-facing strings — and nowhere
else. A rename is then a find-replace, a re-publish, and a GitHub org rename, not a week of
hunting down hardcoded strings."""
